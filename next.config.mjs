/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['i.pinimg.com', 'static1.cbrimages.com', 'static.wikia.nocookie.net', 'images6.fanpop.com'],
	},
};

export default nextConfig;
